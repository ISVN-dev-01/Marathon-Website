# Environment secrets — NEVER commit these
.env
.env.local
.env.production

# Google service account JSON key (if you ever download it locally)
*.json.key
service-account*.json
*-credentials.json

# Node modules
node_modules/
npm-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db
